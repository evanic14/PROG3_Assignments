#include <stdio.h>
#include <string.h>

int main(void)
{
   char s1[] = "Hello";
   char s2[] = "Bye";
   char s3[] = "Hoi";
   int  result = 0;

   result = strlen(???);
   printf("length of s1 is %d\n\n", result);

   result = strcmp(????);
   printf("compare of s1,s2 is %d\n", result);

   result = strcmp(????);
   printf("compare of s3,s2 is %d\n", result);
   
   return 0;
}
